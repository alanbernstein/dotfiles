print_name 3 "- entry"

# TODO: make this cleaner?
source /etc/lsb-release
source /etc/lsb-release-* 

if [[ "$DISTRIB_ID" == *"Ubuntu"* ]] ; then
    DISTRO="ubuntu"
elif [[ "$DISTRIB_ID" == *"CrunchBang"* ]] ; then
    DISTRO="crunchbang"
fi

echo "sourcing dotfiles for distro = $DISTRO"
source $DOT/.paths-$DISTRO
